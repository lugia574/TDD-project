## 즉시 실행 함수(IIFE)

📌 즉시 실행 함수(IIFE)란?
👉 선언과 동시에 실행되는 함수 (function () { ... })();

📌 비동기 즉시 실행 함수란?
👉 (async () => { ... })();
👉 내부에서 await을 사용할 수 있음
👉 useEffect에서 비동기 작업을 할 때 자주 사용됨

즉, useEffect에서 async를 직접 못 쓰니까, IIFE를 활용해서 비동기 작업을 수행하는 방식이야! 🚀

```js
(function () {
  console.log("즉시 실행됨!");
})();

// 예제
useEffect(() => {
  (async () => {
    const next = await loadContentItems({ pageNum: pageLoc, pageTake });
    setItems(next);
  })();
}, [pageLoc]);
```

## +str은 단항(유니어리) 플러스 연산자(Unary Plus Operator) 를 사용한 숫자 변환 방식

이게 뭐 하는 거냐면...
👉 문자열(string)을 숫자(number)로 변환하는 가장 간단한 방법 중 하나야.
👉 암시적(implicit) 형 변환을 활용하는 방식이라 코드가 짧고 가독성이 좋음.

✅ 정리
+str은 문자열을 숫자로 변환하는 간단한 방식

Number(str)과 같은 역할을 하지만 더 짧고 직관적

숫자로 변환할 수 없으면 NaN이 됨 → Number.isNaN()으로 체크 가능

```js
console.log(+"123"); // 123  (문자열 → 숫자로 변환)
console.log(+"3.14"); // 3.14 (소수점도 변환 가능)
console.log(+"0"); // 0
console.log(+null); // 0  (null은 0으로 변환됨)
console.log(+true); // 1  (불리언 true → 1)
console.log(+false); // 0  (불리언 false → 0)
console.log(+""); // 0  (빈 문자열 → 0)
console.log(+"hello"); // NaN (숫자로 변환 불가)

// 예제

export const castNullableStrToNum = (str: string | null): number | null => {
  if (str === null) return null; // null이면 변환하지 않고 그대로 null 반환
  const casted = +str; // 문자열을 숫자로 변환
  if (Number.isNaN(casted)) return null; // 변환 실패(NaN)하면 null 반환
  return casted; // 정상 변환된 숫자 반환
};

// 사용 예제
console.log(castNullableStrToNum("42")); // 42
console.log(castNullableStrToNum("3.14")); // 3.14
console.log(castNullableStrToNum(null)); // null
console.log(castNullableStrToNum("hello")); // null (NaN을 방지)
console.log(castNullableStrToNum("")); // 0
```

## every

useEffect(() => {
const titleOk = validateTitle(title);
const bodyOk = validateBody(body);
const thumbnailSrcOk = typeof thumbnailSrc === "string";
const next = [titleOk, bodyOk, thumbnailSrcOk].every((c) => c === true);

    setStatus(next);

}, [title, body, thumbnailSrc]);
